# Анисимов Дмитрий, гр. 3341
# Тестовая документация: Learn Git Branching, 4 итерация

## Объект тестирования

Сайт:

```text
https://learngitbranching.js.org/?locale=ru_RU
```

Проверяется русская версия тренажёра Learn Git Branching. В рамках 4 итерации реализованы автотесты для заданий 9-12:

- раздел «Перемещаем труды туда-сюда», задание 1: «Введение в Cherry-pick»;
- раздел «Перемещаем труды туда-сюда», задание 2: «Введение в интерактивный Rebase»;
- раздел «Сборная солянка», задание 1: «Выберем один коммит.»;
- раздел «Сборная солянка», задание 2: «Жонглируем коммитами».

Для каждого уровня создано по четыре теста: два позитивных и два негативных.

## Инструменты

- Python
- pytest
- Selenium Remote WebDriver
- Docker Compose
- `selenium/hub`
- `selenium/node-chrome`
- отдельный контейнер `tests` с pytest-тестами

## Общие предусловия

- Установлен Docker.
- Есть доступ к интернету.
- Сайт Learn Git Branching открывается.
- Тесты запускаются из корня папки `4 итерация`.

## Запуск

```powershell
docker compose up --build --abort-on-container-exit
```

Для просмотра браузера во время выполнения тестов:

```text
http://localhost:7900
```

Дополнительный ввод при открытии просмотра не нужен.

## Назначение основных каталогов и файлов

Основная структура проекта:

```text
Dockerfile
docker-compose.yml
requirements.txt
pytest.ini
TEST_DOCUMENTATION.md
tests/
```

Назначение основных каталогов и файлов:

- `Dockerfile` — описывает контейнер `tests`, внутри которого устанавливаются Python-зависимости и запускается pytest;
- `docker-compose.yml` — описывает три контейнера: Selenium Hub, Chrome node и контейнер с тестами;
- `requirements.txt` — содержит библиотеки, необходимые для запуска автотестов;
- `pytest.ini` — хранит настройки pytest и зарегистрированные маркеры тестов;
- `TEST_DOCUMENTATION.md` — описание реализованных тестов 4 итерации;
- `tests/` — каталог с автотестами, фикстурами и Page Object классом;
- `tests/e2e/` — тестовые сценарии, каждый файл соответствует отдельному тест-кейсу;
- `tests/pages/` — вспомогательные классы для работы со страницей Learn Git Branching.

### Page Object классы

В проекте используется Page Object подход. Общая логика работы с сайтом вынесена в файл:

```text
tests/pages/learn_git_branching_page.py
```

Класс `LearnGitBranchingPage` отвечает за повторяющиеся действия на сайте:

- открытие нужного уровня;
- пропуск стартовых и уровневых инструкций;
- выбор уровня через интерфейс;
- ввод команд в консоль тренажёра;
- обработку интерактивного rebase через модальное окно сайта;
- ожидание завершения анимаций и команд;
- проверку успешного прохождения уровня;
- получение текста графа коммитов для проверок.

Благодаря этому сами тесты в `tests/e2e/` остаются короткими: в них описан сценарий конкретного тест-кейса, а техническая работа с интерфейсом находится в Page Object.

### Фикстуры pytest

Фикстуры расположены в файле:

```text
tests/conftest.py
```

Основные фикстуры:

- `selenium_url` — получает адрес Selenium Grid из переменной окружения `SELENIUM_REMOTE_URL`;
- `driver` — создаёт Selenium Remote WebDriver, подключённый к Chrome node в Docker;
- `lgb` — создаёт объект `LearnGitBranchingPage`, через который тесты работают с сайтом.

Перед созданием браузера выполняется ожидание готовности Selenium Grid. Это нужно, чтобы тесты не стартовали раньше, чем контейнеры `selenium/hub` и `selenium/node-chrome` будут готовы принимать команды.

## Структура тестов

```text
tests/e2e/
```

В названии файла используется:

- `p` — позитивный сценарий;
- `n` — негативный сценарий;
- `p1`, `p2` — два позитивных сценария;
- `n1`, `n2` — два негативных сценария.

## Реализованные тест-кейсы

| ID | Файл | Уровень сайта | Тип | Проверка |
|---|---|---|---|---|
| TC-09-P1 | `test_tc_09_p1_cherry_pick_all_commits.py` | `move1` | Позитивный | `git cherry-pick C3 C4 C7` переносит нужные коммиты одной командой. |
| TC-09-P2 | `test_tc_09_p2_cherry_pick_commits_separately.py` | `move1` | Позитивный | Нужные коммиты переносятся отдельными командами cherry-pick. |
| TC-09-N1 | `test_tc_09_n1_cherry_pick_missing_commit.py` | `move1` | Негативный | Неполный набор коммитов не засчитывает уровень. |
| TC-09-N2 | `test_tc_09_n2_cherry_pick_unknown_commit.py` | `move1` | Негативный | Несуществующий коммит `C99` вызывает ошибку и не меняет граф. |
| TC-10-P1 | `test_tc_10_p1_interactive_rebase_solution.py` | `move2` | Позитивный | Интерактивный rebase выставляет порядок `C3`, `C5`, `C4`. |
| TC-10-P2 | `test_tc_10_p2_interactive_rebase_from_commit.py` | `move2` | Позитивный | Интерактивный rebase работает от эквивалентной базы `C1`. |
| TC-10-N1 | `test_tc_10_n1_interactive_rebase_wrong_order.py` | `move2` | Негативный | Неверный порядок коммитов не засчитывает уровень. |
| TC-10-N2 | `test_tc_10_n2_interactive_rebase_unknown_base.py` | `move2` | Негативный | Несуществующая база `wrongBranch` вызывает ошибку. |
| TC-11-P1 | `test_tc_11_p1_grab_one_commit_rebase.py` | `mixed1` | Позитивный | Один нужный коммит выбирается через интерактивный rebase. |
| TC-11-P2 | `test_tc_11_p2_grab_one_commit_cherry_pick.py` | `mixed1` | Позитивный | Один нужный коммит переносится через cherry-pick. |
| TC-11-N1 | `test_tc_11_n1_grab_debug_commit.py` | `mixed1` | Негативный | Выбор коммита `debug` не приводит к целевому состоянию. |
| TC-11-N2 | `test_tc_11_n2_rebase_all_bugfix_history.py` | `mixed1` | Негативный | Перенос всей истории `bugFix` не соответствует цели уровня. |
| TC-12-P1 | `test_tc_12_p1_juggling_commits_rebase.py` | `mixed2` | Позитивный | Полный сценарий с rebase, amend и повторным rebase проходит уровень. |
| TC-12-P2 | `test_tc_12_p2_juggling_commits_checkout_rebase.py` | `mixed2` | Позитивный | Альтернативный финальный rebase после checkout `main` проходит уровень. |
| TC-12-N1 | `test_tc_12_n1_amend_wrong_commit.py` | `mixed2` | Негативный | `commit --amend` на исходном HEAD изменяет не тот коммит. |
| TC-12-N2 | `test_tc_12_n2_juggling_wrong_rebase_order.py` | `mixed2` | Негативный | Неверный порядок интерактивного rebase не засчитывает уровень. |

## Ожидаемый результат полного запуска

```text
16 passed
```

Последний полный Docker-прогон:

```text
16 passed in 841.83s (0:14:01)
```
